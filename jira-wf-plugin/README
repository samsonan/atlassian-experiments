# JIRA Workflow Post Function Plugin

Jira Workflow Post Function Plugin which adds one Post Function, which allows to set selected Date/Time Field value to Current Date/Time for selected Issue Type.

On init, user is requested to input two values:
- Issue Type (e.g. Feature)
- Date field name

On transition, if the value is updated, plugin also adds new comment, providing addition information on current state and transition
