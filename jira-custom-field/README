# JIRA Custom Field Plugin

Jira Custom Field Plugin which provides two custom fields:

1. Read-only field which shows Assigned User
2. Read-only field which shows Parent Fix Versions. Not visible for non-subtasks.
